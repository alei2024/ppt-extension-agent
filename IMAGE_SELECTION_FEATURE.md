# 图片选择功能实现总结

## 完成的功能

### 1. 图片选择功能 ✅

- 用户可以点击图片进行选择/取消选择
- 选中的图片会有视觉反馈（边框高亮、背景色变化）
- 选中的图片会显示"✓ 已选中"标记

### 2. 图片过滤 ✅

- 只显示有文字内容的图片（有 `description` 或 `ocr_text`）
- 没有文字的图片不会显示在前端页面

### 3. 知识扩充集成 ✅

- 选中的图片信息会与选中的文本框一起发送给后端
- 后端会将图片描述和OCR文字添加到扩充内容中
- "扩展整页"功能也会自动包含所有有文字的图片

### 4. 全选功能更新 ✅

- "全选"按钮现在会同时选择所有文本框和有文字的图片
- "清除选择"按钮会清除所有选择（文本框和图片）

## 前端更新

### App.tsx

1. **添加图片选择状态**：
   ```typescript
   const [selectedImages, setSelectedImages] = useState<number[]>([])
   ```

2. **添加图片选择处理函数**：
   ```typescript
   const handleImageSelect = (imageId: number) => {
     // 切换图片选择状态
   }
   ```

3. **更新知识扩充逻辑**：
   - 收集选中的图片信息（description和ocr_text）
   - 将图片信息添加到context中一起发送

4. **更新全选/清除选择**：
   - 全选：同时选择所有文本框和有文字的图片
   - 清除：清除所有选择

### PPTViewer.tsx

1. **过滤图片**：
   ```typescript
   const imagesWithText = slide.images?.filter(
     (img) => img.description || img.ocr_text
   ) || []
   ```

2. **添加选择功能**：
   - 图片可点击选择
   - 选中状态有视觉反馈
   - 显示"✓ 已选中"标记

3. **更新Props**：
   - 添加 `selectedImages` 和 `onImageSelect` props

## 后端支持

后端已经支持图片信息：

1. **知识扩充服务** (`services/knowledge_expander.py`)：
   - 检查context中的images信息
   - 将图片描述和OCR文字添加到扩充内容中

2. **格式化上下文** (`_format_context` 方法)：
   - 将图片信息格式化为字符串
   - 添加到上下文中

## 使用方式

### 1. 选择图片

- 点击图片即可选择/取消选择
- 选中的图片会有明显的视觉反馈

### 2. 扩展选中内容

- 选择文本框和/或图片
- 点击"扩展选中"按钮
- 系统会将选中的文本框和图片信息一起发送给LLM进行扩充

### 3. 扩展整页

- 点击"扩展整页"按钮
- 系统会自动包含所有文本框和有文字的图片

## 数据流

```
用户选择图片
    ↓
前端收集图片信息（description, ocr_text）
    ↓
与文本框内容一起发送到后端
    ↓
后端知识扩充服务
    ├─ 提取图片描述和OCR文字
    ├─ 添加到扩充内容中
    └─ 发送给LLM生成扩展内容
    ↓
返回扩展结果（包含图片相关信息）
```

## 注意事项

1. **图片过滤**：
   - 只有有文字内容的图片才会显示
   - 如果图片既没有description也没有ocr_text，不会显示

2. **选择状态**：
   - 图片选择和文本框选择是独立的
   - 可以只选择图片，也可以只选择文本框，或者两者都选

3. **扩展要求**：
   - 至少需要选择一个文本框或一张图片才能扩展
   - 如果什么都没选，"扩展选中"按钮会被禁用

4. **图片路径**：
   - 图片通过静态文件服务提供：`http://localhost:8000/uploads/{file_path}`
   - 确保静态文件服务已正确配置

## 测试建议

1. **测试图片选择**：
   - 上传包含图片的PPT
   - 点击图片，检查是否有视觉反馈
   - 检查"✓ 已选中"标记是否显示

2. **测试图片过滤**：
   - 上传包含有文字和无文字图片的PPT
   - 确认只有有文字的图片显示

3. **测试知识扩充**：
   - 选择文本框和图片
   - 点击"扩展选中"
   - 检查扩展内容是否包含图片信息

4. **测试全选功能**：
   - 点击"全选"按钮
   - 确认所有文本框和有文字的图片都被选中
